{
  "frontend/src/types/trip.ts": "TypeScript type definitions for TripRecord, TripFormValues, TripFormErrors, DashboardStats, and the new TripStatus union type ('Complete' | 'Cancel' | 'Pending'); TripRecord now includes a required `status: TripStatus` field.",
  "frontend/src/hooks/useTripData.ts": "localStorage persistence hook for trip records with add/update/delete operations; now includes `updateTripStatus` function for inline status changes and defaults missing `status` field to 'Pending' for backward compatibility.",
  "frontend/src/hooks/useTripForm.ts": "Form state management hook with validation, edit-mode population, and submit handling; now preserves the existing trip's `status` on edit submission and defaults to 'Pending' for new trips.",
  "frontend/src/components/TripForm.tsx": "Trip entry/edit form component with all fields including vehicle dropdown; now always shows a back/cancel button (labeled \"← Back to Dashboard\" for new entries, \"Cancel\" for edits), and the vehicle management link text updated to reference the Vehicles tab instead of an anchor link.",
  "frontend/src/components/TripTable.tsx": "Searchable trip records table with edit/delete actions and a new inline Status column; each row shows three small pill buttons (Pending/amber, Complete/green, Cancel/red) with the active status highlighted, wired to `onStatusChange` for immediate localStorage updates.",
  "frontend/src/utils/exportUtils.ts": "CSV and PDF export utilities for trip reports; both exports now include a \"Vehicle Number\" column after Order ID in the trip detail table rows, with the summary section unchanged.",
  "frontend/src/hooks/useAuth.ts": "Custom React hook managing session-based authentication state using sessionStorage, with login (validates hardcoded credentials: username 'vsat', password '0558') and logout functions.",
  "frontend/src/components/Login.tsx": "Centered login card component with username and password fields, credential validation via callback, inline error display for invalid credentials, and a loading spinner during submission.",
  "frontend/src/App.tsx": "Main app component with state-based multi-page navigation; now destructures `updateTripStatus` from `useTripData` and passes `onStatusChange` handler to `TripTable` for inline trip status updates.",
  "frontend/src/components/Header.tsx": "App header with branding, logout button, and a persistent navigation bar with four tabs (Dashboard, Create Trip, Reports, Vehicles); the horizontal scroll/sliding bar has been removed from the nav element while keeping all navigation functionality intact.",
  "frontend/src/components/DailyReport.tsx": "Daily report component that now shows a three-card status breakdown row (Pending, Completed, Cancelled) between the summary stat cards and the trip details table, visible only after the report is generated.",
  "frontend/src/components/DateRangeReport.tsx": "Date range report component that now shows a three-card status breakdown row (Pending, Completed, Cancelled) between the summary stat cards and the trip details table, visible only after the report is generated.",
  "frontend/src/hooks/useVehicleList.ts": "Custom hook that manages a list of vehicle numbers persisted in localStorage under the key 'vehicleList'. Exposes `vehicles` (string[]), `addVehicle` (with duplicate check), and `deleteVehicle` functions, defaulting to an empty array if the key is absent.",
  "frontend/src/components/VehicleManagement.tsx": "Vehicle Management card component that lets authenticated users add new vehicle numbers (with empty/duplicate validation and Enter-key support), view all saved vehicles in a table, and delete entries via a confirmation dialog using AlertDialog.",
  "frontend/src/hooks/useDashboardStats.ts": "Extended dashboard stats hook that now also computes and returns a `statusBreakdown` object with `pendingCount`, `completedCount`, and `cancelledCount` derived from the filtered trips, alongside the existing stats and chartData.",
  "frontend/src/components/Dashboard.tsx": "Dashboard component that now renders a second row of three status breakdown cards (Pending ⏳, Completed ✅, Cancelled ❌) below the four main stat cards, all respecting the active date/month filter."
}